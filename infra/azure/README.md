# GCC Azure Infrastructure

Deploy with:

```bash
./scripts/azure/deploy-infra.sh
./scripts/azure/build-push-deploy.sh
```

See `docs/azure-hosting-migration.md` for full migration runbook, env inventory, DNS cutover, and Intuit guidance.
