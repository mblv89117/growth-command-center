/**
 * Kept as a thin re-export pointer; executable suite is
 * scripts/microsoft-native-cutover.test.mjs (node:test, no vitest).
 * Excluded from Next/tsconfig so Azure Docker builds stay clean.
 */
export {};
